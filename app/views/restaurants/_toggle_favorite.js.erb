var restaurantId = '<%= @restaurant.id%>'
function toggleLikeButton(restaurantId, innerHTML) {
  let likeButton = document.querySelector(`.like-icon[data-id='${restaurantId}']`)
  likeButton.classList.toggle('red');
}
toggleLikeButton(restaurantId);

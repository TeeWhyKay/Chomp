var restaurantId = '<%= @restaurant.id %>'

function toggleLikeButton(restaurantId, innerHTML) {
  let likeButton = document.querySelector(`.like-icon[data-id='${restaurantId}']`)
  likeButton.classList.toggle('fav-red');
}
toggleLikeButton(restaurantId);

function toggleLikeButton(restaurantId, favoritedRestaurant) {
  let likeButton = document.querySelector(`.like-icon[data-id='${restaurantId}']`)
  likeButton.classList.toggle('fav-red');
  if (likeButton.classList.contains('fav-red')) {
    const favoriteList = document.querySelector('.favorites');
    favoriteList.insertAdjacentHTML('beforeend', favoritedRestaurant)
  }
  else {
    document.querySelector(`.favorite[data-id='${restaurantId}']`).remove()
  }
toggleLikeButton(restaurantId, '<%= j render "restaurants/restaurant", restaurant: @restaurant
